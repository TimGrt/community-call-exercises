---

# Not displaying warning about role names yet, see https://ansible-lint.readthedocs.io/rules/role-name/
skip_list:
  - role-name

exclude_paths:
  - .github/
  - exercise-best-practices/  # This exercise contains loads of bad practices, not linting this in CI
